{% extends "admin/base.html.twig" %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-header with-border">
                    <h3 class="box-title">Bordered Table</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table class="table table-bordered">
                        <tr>
                            <th style="width: 10px">#</th>
                            <th>{{'username' | trans}}</th>
                            <th></th>
                            <th>{{'action' | trans}}</th>
                        </tr>
                        {% for item in list %}
                            <tr>
                                <td>{{item.id}}</td>
                                <td>{{item.username}}</td>
                                <td></td>
                                <td><a class="btn btn-danger" href="{{path('user_remove',{'id': item.id} )}}"><i class="fa fa-close"></i></a></td>
                            </tr>
                        {% endfor %}
                    </table>
                </div>
                <!-- /.box-body -->
                
                {{ knp_pagination_render(list) }}
                  
            </div>
            <!-- /.box -->

        </div>
        <!-- /.col -->
    </div>

{% endblock %}